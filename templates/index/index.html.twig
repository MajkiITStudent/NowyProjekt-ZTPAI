{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Mainpage{% endblock %}

{% block body %}
<h1>{{ liczba }}</h1>
    <h1>{{ liczba }}</h1>
    <h1>{{ liczba }}</h1>
    <h1>{{ liczba }}</h1>
{% endblock %}
